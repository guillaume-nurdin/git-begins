git clone https://github.com/YOUR-USERNAME/YOUR-REPOSITORY

git push 

git push origin <> : Envoie les commits de la branche spécifiée vers le dépôt distant nommé "origin". Remplace <branche> par le nom de la branche que tu souhaites pousser.

git push --force 